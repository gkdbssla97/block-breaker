package model.domain;

import java.awt.*;

import static util.Constant.*;

public class Bar extends MObject{
    int x = CANVAS_WIDTH / 2;
    int y = CANVAS_HEIGHT - 100;
    int width = BAR_WIDTH;
    int height = BAR_HEIGHT;

    @Override
    void draw(Graphics g) {

    }
}
